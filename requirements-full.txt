# "Full" server image: adds open-vocabulary (YOLOE-26), server-side training, and the
# LangGraph + Groq chatbot on top of the lean runtime. Heavier (torch/ultralytics +
# langchain); enable only where the extra weight is okay.
-r requirements.txt
ultralytics>=8.4.0
# Chatbot — LangGraph multi-agent text-to-SQL over the detections DB, Groq LLM.
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
