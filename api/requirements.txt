# Vercel serverless function deps — logging + torch-free chatbot.
# (NO onnxruntime/opencv/ultralytics/torch — all inference runs client-side in WebGPU.)
fastapi>=0.115.0
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0
pydantic>=2.7.0
python-dotenv>=1.0.0
# Chatbot: LangGraph multi-agent text-to-SQL over Groq (no torch). Gated on GROQ_API_KEY.
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
